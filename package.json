{
  "name": "frioleros",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify": "node scripts/verify-build.mjs",
    "verify:source": "tsc --noEmit --noUnusedLocals --noUnusedParameters && node scripts/verify-source.mjs",
    "sume:map": "node scripts/update-sume-map.mjs",
    "verify:sume": "node --test scripts/sume-map-guard.test.mjs && node scripts/verify-sume.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.7.0"
  }
}
